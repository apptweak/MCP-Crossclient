{
  "name": "mcp-crossclient",
  "private": true,
  "version": "0.1.1",
  "description": "Official AppTweak MCP integrations for Cursor, Claude Code, and Codex",
  "license": "GPL-3.0-only",
  "type": "module",
  "scripts": {
    "prepare": "lefthook install",
    "secrets": "./scripts/gitleaks.sh git --config .gitleaks.toml --verbose",
    "secrets:staged": "./scripts/gitleaks.sh protect --staged --config .gitleaks.toml --verbose"
  },
  "devDependencies": {
    "@nogoo9/gitleaks": "^8.30.1-post.2",
    "lefthook": "^2.1.9"
  }
}
